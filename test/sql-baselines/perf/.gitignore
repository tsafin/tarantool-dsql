# Ignore generated perf-trail CSV files (one per CI run).
# Populate this directory manually from CI artifacts or with PERF_OUT_DIR.
*.csv

# But keep the committed sample for documentation purposes.
!sample.csv
