/* Generated by tools/vdbe_codegen.py - do not edit */
#include "vdbe_opcodes_generated.h"

/*
 * Complete VDBE dispatch loop generated from opcodes.yaml
 *
 * This dispatch replaces the inline EXECUTE() macros in vdbe.c.
 * It supports both computed-goto (SQL_USE_GOTO) and switch fallback modes.
 *
 * External handlers (extracted to vdbe_ops_*.c) are called with:
 *   int vdbe_op_xxx(Vdbe *p, Op *pOp, Mem *aMem)
 *
 * Return values:
 *   0 = Success, continue to next instruction
 *  -1 = Error, jump to abort_due_to_error
 *   1 = Special: comparison ops jump to P2, OP_ResultRow returns SQL_ROW
 */

#ifdef SQL_USE_GOTO
/* Generated dispatch loop - computed-goto version */
/* This is included as part of sqlVdbeExec() when SQL_USE_GOTO is defined */

/* Dispatch table for computed-goto - indexed by opcode ID */
static const void * const dispatch_table[] = {
    &&Exec_OP_Savepoint, /* 0 */
    &&Exec_OP_SorterNext, /* 1 */
    &&Exec_OP_PrevIfOpen, /* 2 */
    &&Exec_OP_NextIfOpen, /* 3 */
    &&Exec_OP_Prev, /* 4 */
    &&Exec_OP_Or, /* 5 */
    &&Exec_OP_And, /* 6 */
    &&Exec_OP_Not, /* 7 */
    &&Exec_OP_Next, /* 8 */
    &&Exec_OP_Goto, /* 9 */
    &&Exec_OP_SetDiag, /* 10 */
    &&Exec_OP_Gosub, /* 11 */
    &&Exec_OP_InitCoroutine, /* 12 */
    &&Exec_OP_Ne, /* 13 */
    &&Exec_OP_Eq, /* 14 */
    &&Exec_OP_Gt, /* 15 */
    &&Exec_OP_Le, /* 16 */
    &&Exec_OP_Lt, /* 17 */
    &&Exec_OP_Ge, /* 18 */
    &&Exec_OP_ElseNotEq, /* 19 */
    &&Exec_OP_BitAnd, /* 20 */
    &&Exec_OP_BitOr, /* 21 */
    &&Exec_OP_ShiftLeft, /* 22 */
    &&Exec_OP_ShiftRight, /* 23 */
    &&Exec_OP_Add, /* 24 */
    &&Exec_OP_Subtract, /* 25 */
    &&Exec_OP_Multiply, /* 26 */
    &&Exec_OP_Divide, /* 27 */
    &&Exec_OP_Remainder, /* 28 */
    &&Exec_OP_Concat, /* 29 */
    &&Exec_OP_Yield, /* 30 */
    &&Exec_OP_BitNot, /* 31 */
    &&Exec_OP_MustBeInt, /* 32 */
    &&Exec_OP_Jump, /* 33 */
    &&Exec_OP_Once, /* 34 */
    &&Exec_OP_IfNot, /* 35 */
    &&Exec_OP_SeekLT, /* 36 */
    &&Exec_OP_SeekGT, /* 37 */
    &&Exec_OP_SeekGE, /* 38 */
    &&Exec_OP_Found, /* 39 */
    &&Exec_OP_Last, /* 40 */
    &&Exec_OP_SorterSort, /* 41 */
    &&Exec_OP_Sort, /* 42 */
    &&Exec_OP_Rewind, /* 43 */
    &&Exec_OP_IdxGE, /* 44 */
    &&Exec_OP_Program, /* 45 */
    &&Exec_OP_IfPos, /* 46 */
    &&Exec_OP_IfNotZero, /* 47 */
    &&Exec_OP_String8, /* 48 */
    &&Exec_OP_DecrJumpZero, /* 49 */
    &&Exec_OP_Init, /* 50 */
    &&Exec_OP_Return, /* 51 */
    &&Exec_OP_EndCoroutine, /* 52 */
    &&Exec_OP_Halt, /* 53 */
    &&Exec_OP_Integer, /* 54 */
    &&Exec_OP_Bool, /* 55 */
    &&Exec_OP_Int64, /* 56 */
    &&Exec_OP_String, /* 57 */
    &&Exec_OP_Null, /* 58 */
    &&Exec_OP_Blob, /* 59 */
    &&Exec_OP_Variable, /* 60 */
    &&Exec_OP_Move, /* 61 */
    &&Exec_OP_Copy, /* 62 */
    &&Exec_OP_SCopy, /* 63 */
    &&Exec_OP_ResultRow, /* 64 */
    &&Exec_OP_SkipLoad, /* 65 */
    &&Exec_OP_BuiltinFunction, /* 66 */
    &&Exec_OP_FunctionByName, /* 67 */
    &&Exec_OP_AddImm, /* 68 */
    &&Exec_OP_Cast, /* 69 */
    &&Exec_OP_Array, /* 70 */
    &&Exec_OP_Map, /* 71 */
    &&Exec_OP_Getitem, /* 72 */
    &&Exec_OP_Permutation, /* 73 */
    &&Exec_OP_Compare, /* 74 */
    &&Exec_OP_If, /* 75 */
    &&Exec_OP_Column, /* 76 */
    &&Exec_OP_FetchByName, /* 77 */
    &&Exec_OP_Fetch, /* 78 */
    &&Exec_OP_ApplyType, /* 79 */
    &&Exec_OP_MakeRecord, /* 80 */
    &&Exec_OP_Count, /* 81 */
    &&Exec_OP_CreateForeignKey, /* 82 */
    &&Exec_OP_CreateCheck, /* 83 */
    &&Exec_OP_DropTupleForeignKey, /* 84 */
    &&Exec_OP_DropTupleCheck, /* 85 */
    &&Exec_OP_DropFieldForeignKey, /* 86 */
    &&Exec_OP_DropFieldCheck, /* 87 */
    &&Exec_OP_AddFuncDefault, /* 88 */
    &&Exec_OP_CheckViewReferences, /* 89 */
    &&Exec_OP_TransactionBegin, /* 90 */
    &&Exec_OP_TransactionCommit, /* 91 */
    &&Exec_OP_TransactionRollback, /* 92 */
    &&Exec_OP_TTransaction, /* 93 */
    &&Exec_OP_IteratorOpen, /* 94 */
    &&Exec_OP_OpenSpace, /* 95 */
    &&Exec_OP_OpenTEphemeral, /* 96 */
    &&Exec_OP_SorterOpen, /* 97 */
    &&Exec_OP_SequenceTest, /* 98 */
    &&Exec_OP_OpenPseudo, /* 99 */
    &&Exec_OP_Close, /* 100 */
    &&Exec_OP_SeekLE, /* 101 */
    &&Exec_OP_NoConflict, /* 102 */
    &&Exec_OP_NotFound, /* 103 */
    &&Exec_OP_Sequence, /* 104 */
    &&Exec_OP_NextSystemSpaceId, /* 105 */
    &&Exec_OP_NextIdEphemeral, /* 106 */
    &&Exec_OP_FCopy, /* 107 */
    &&Exec_OP_Delete, /* 108 */
    &&Exec_OP_ResetCount, /* 109 */
    &&Exec_OP_SorterCompare, /* 110 */
    &&Exec_OP_SorterData, /* 111 */
    &&Exec_OP_RowData, /* 112 */
    &&Exec_OP_NullRow, /* 113 */
    &&Exec_OP_SorterInsert, /* 114 */
    &&Exec_OP_IdxReplace, /* 115 */
    &&Exec_OP_IdxInsert, /* 116 */
    &&Exec_OP_Update, /* 117 */
    &&Exec_OP_SInsert, /* 118 */
    &&Exec_OP_SDelete, /* 119 */
    &&Exec_OP_IdxDelete, /* 120 */
    &&Exec_OP_IdxLE, /* 121 */
    &&Exec_OP_IdxGT, /* 122 */
    &&Exec_OP_Real, /* 123 */
    &&Exec_OP_Decimal, /* 124 */
    &&Exec_OP_IdxLT, /* 125 */
    &&Exec_OP_Clear, /* 126 */
    &&Exec_OP_ResetSorter, /* 127 */
    &&Exec_OP_RenameTable, /* 128 */
    &&Exec_OP_LoadAnalysis, /* 129 */
    &&Exec_OP_Param, /* 130 */
    &&Exec_OP_OffsetLimit, /* 131 */
    &&Exec_OP_AggStep, /* 132 */
    &&Exec_OP_AggFinal, /* 133 */
    &&Exec_OP_Expire, /* 134 */
    &&Exec_OP_GenSpaceid, /* 135 */
    &&Exec_OP_SetSession, /* 136 */
    &&Exec_OP_ShowCreateTable, /* 137 */
    &&Exec_OP_Noop, /* 138 */
    &&Exec_OP_Explain, /* 139 */
    &&Exec_OP_Noop, /* 140 - unused */
    &&Exec_OP_Noop, /* 141 - unused */
    &&Exec_OP_Noop, /* 142 - unused */
    &&Exec_OP_Noop, /* 143 - unused */
    &&Exec_OP_Noop, /* 144 - unused */
    &&Exec_OP_Noop, /* 145 - unused */
    &&Exec_OP_Noop, /* 146 - unused */
    &&Exec_OP_Noop, /* 147 - unused */
    &&Exec_OP_Noop, /* 148 - unused */
    &&Exec_OP_Noop, /* 149 - unused */
    &&Exec_OP_Noop, /* 150 - unused */
    &&Exec_OP_Noop, /* 151 - unused */
    &&Exec_OP_Noop, /* 152 - unused */
    &&Exec_OP_Noop, /* 153 - unused */
    &&Exec_OP_Noop, /* 154 - unused */
    &&Exec_OP_Noop, /* 155 - unused */
    &&Exec_OP_Noop, /* 156 - unused */
    &&Exec_OP_Noop, /* 157 - unused */
    &&Exec_OP_Noop, /* 158 - unused */
    &&Exec_OP_Noop, /* 159 - unused */
    &&Exec_OP_Noop, /* 160 - unused */
    &&Exec_OP_Noop, /* 161 - unused */
    &&Exec_OP_Noop, /* 162 - unused */
    &&Exec_OP_Noop, /* 163 - unused */
    &&Exec_OP_Noop, /* 164 - unused */
    &&Exec_OP_Noop, /* 165 - unused */
    &&Exec_OP_Noop, /* 166 - unused */
    &&Exec_OP_Noop, /* 167 - unused */
    &&Exec_OP_Noop, /* 168 - unused */
    &&Exec_OP_Noop, /* 169 - unused */
    &&Exec_OP_Noop, /* 170 - unused */
    &&Exec_OP_Noop, /* 171 - unused */
    &&Exec_OP_Noop, /* 172 - unused */
    &&Exec_OP_Noop, /* 173 - unused */
    &&Exec_OP_IsNull, /* 174 */
    &&Exec_OP_NotNull, /* 175 */
};

/* Labels for each opcode - used in computed-goto dispatch */
Exec_OP_Concat: {
    /* Opcode: CONCAT - String concatenation r[P3]=r[P1]+r[P2] */
    int handler_rc = vdbe_op_concat(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Cast: {
    /* Opcode: CAST - Explicit type cast r[P1] */
    int handler_rc = vdbe_op_cast(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_ApplyType: {
    /* Opcode: APPLYTYPE - Apply type checking r[P1@P2] */
    int handler_rc = vdbe_op_applytype(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_MakeRecord: {
    /* Opcode: MAKERECORD - Create msgpack record r[P3]=mkrec(r[P1@P2]) */
    int handler_rc = vdbe_op_makerecord(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_AggStep: {
    /* Opcode: AGGSTEP - Aggregate step accum=r[P3] step(r[P2@P1]) */
    int handler_rc = vdbe_op_aggstep(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_AggFinal: {
    /* Opcode: AGGFINAL - Aggregate finalize accum=r[P1] */
    int handler_rc = vdbe_op_aggfinal(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_ResultRow: {
    /* Opcode: RESULTROW - Return result row output=r[P1@P2] */
    int handler_rc = vdbe_op_resultrow(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Column: {
    /* Opcode: COLUMN - Extract column from cursor r[P3]=PX */
    int handler_rc = vdbe_op_column(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_RowData: {
    /* Opcode: ROWDATA - Get complete row data r[P2]=data */
    int handler_rc = vdbe_op_rowdata(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Rewind: {
    /* Opcode: REWIND - Rewind cursor to first row */
    int handler_rc = vdbe_op_rewind(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Last: {
    /* Opcode: LAST - Position cursor to last row */
    int handler_rc = vdbe_op_last(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Next: {
    /* Opcode: NEXT - Advance cursor to next row */
    int handler_rc = vdbe_op_next(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_NextIfOpen: {
    /* Opcode: NEXTIFOPEN - Advance cursor if open */
    int handler_rc = vdbe_op_nextifopen(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Prev: {
    /* Opcode: PREV - Move cursor to previous row */
    int handler_rc = vdbe_op_prev(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_PrevIfOpen: {
    /* Opcode: PREVIFOPEN - Move cursor to previous row if open */
    int handler_rc = vdbe_op_previfopen(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_SeekLE: {
    /* Opcode: SEEKLE - Seek to LE key position key=r[P3@P4] */
    int handler_rc = vdbe_op_seekle(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_SeekGT: {
    /* Opcode: SEEKGT - Seek to GT key position key=r[P3@P4] */
    int handler_rc = vdbe_op_seekgt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_SeekGE: {
    /* Opcode: SEEKGE - Seek to GE key position key=r[P3@P4] */
    int handler_rc = vdbe_op_seekge(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_SeekLT: {
    /* Opcode: SEEKLT - Seek to LT key position key=r[P3@P4] */
    int handler_rc = vdbe_op_seeklt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_IdxGE: {
    /* Opcode: IDXGE - Index comparison GE key=r[P3@P4] */
    int handler_rc = vdbe_op_idxge(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_IdxGT: {
    /* Opcode: IDXGT - Index comparison GT key=r[P3@P4] */
    int handler_rc = vdbe_op_idxgt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_IdxLE: {
    /* Opcode: IDXLE - Index comparison LE key=r[P3@P4] */
    int handler_rc = vdbe_op_idxle(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_IdxLT: {
    /* Opcode: IDXLT - Index comparison LT key=r[P3@P4] */
    int handler_rc = vdbe_op_idxlt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Found: {
    /* Opcode: FOUND - Index lookup - exact match key=r[P3@P4] */
    int handler_rc = vdbe_op_found(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_NotFound: {
    /* Opcode: NOTFOUND - Index lookup - not found key=r[P3@P4] */
    int handler_rc = vdbe_op_notfound(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_NoConflict: {
    /* Opcode: NOCONFLICT - Index conflict check key=r[P3@P4] */
    int handler_rc = vdbe_op_noconflict(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_IdxInsert: {
    /* Opcode: IDXINSERT - Insert index entry key=r[P1] */
    int handler_rc = vdbe_op_idxinsert(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_IdxReplace: {
    /* Opcode: IDXREPLACE - Replace index entry key=r[P1] */
    int handler_rc = vdbe_op_idxreplace(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Delete: {
    /* Opcode: DELETE - Delete record from table key=r[P1] */
    int handler_rc = vdbe_op_delete(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Update: {
    /* Opcode: UPDATE - Update record in table key=r[P1] */
    int handler_rc = vdbe_op_update(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_SInsert: {
    /* Opcode: SINSERT - Insert into system space space id = P1, key = r[P2] */
    int handler_rc = vdbe_op_sinsert(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_SDelete: {
    /* Opcode: SDELETE - Delete from system space space id = P1, key = r[P2] */
    int handler_rc = vdbe_op_sdelete(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_IdxDelete: {
    /* Opcode: IDXDELETE - Delete index entry key=r[P2@P3] */
    int handler_rc = vdbe_op_idxdelete(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Add: {
    /* Opcode: ADD - Addition r[P3]=r[P1]+r[P2] */
    int handler_rc = vdbe_op_add(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Subtract: {
    /* Opcode: SUBTRACT - Subtraction r[P3]=r[P2]-r[P1] */
    int handler_rc = vdbe_op_subtract(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Multiply: {
    /* Opcode: MULTIPLY - Multiplication r[P3]=r[P1]*r[P2] */
    int handler_rc = vdbe_op_multiply(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Divide: {
    /* Opcode: DIVIDE - Division r[P3]=r[P2]/r[P1] */
    int handler_rc = vdbe_op_divide(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Remainder: {
    /* Opcode: REMAINDER - Modulo r[P3]=r[P2]%r[P1] */
    int handler_rc = vdbe_op_remainder(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Eq: {
    /* Opcode: EQ - Equality comparison if r[P3]==r[P1] goto P2 */
    int handler_rc = vdbe_op_eq(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Ne: {
    /* Opcode: NE - Not equal comparison if r[P3]!=r[P1] goto P2 */
    int handler_rc = vdbe_op_ne(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Lt: {
    /* Opcode: LT - Less than comparison if r[P3]<r[P1] goto P2 */
    int handler_rc = vdbe_op_lt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Le: {
    /* Opcode: LE - Less or equal comparison if r[P3]<=r[P1] goto P2 */
    int handler_rc = vdbe_op_le(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Gt: {
    /* Opcode: GT - Greater than comparison if r[P3]>r[P1] goto P2 */
    int handler_rc = vdbe_op_gt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Ge: {
    /* Opcode: GE - Greater or equal comparison if r[P3]>=r[P1] goto P2 */
    int handler_rc = vdbe_op_ge(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_And: {
    /* Opcode: AND - Logical AND r[P3]=(r[P1] && r[P2]) */
    int handler_rc = vdbe_op_and(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Or: {
    /* Opcode: OR - Logical OR r[P3]=(r[P1] || r[P2]) */
    int handler_rc = vdbe_op_or(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Not: {
    /* Opcode: NOT - Logical NOT r[P2]= !r[P1] */
    int handler_rc = vdbe_op_not(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_BitAnd: {
    /* Opcode: BITAND - Bitwise AND r[P3]=r[P1]&r[P2] */
    int handler_rc = vdbe_op_bitand(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_BitOr: {
    /* Opcode: BITOR - Bitwise OR r[P3]=r[P1]|r[P2] */
    int handler_rc = vdbe_op_bitor(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_BitNot: {
    /* Opcode: BITNOT - Bitwise NOT r[P1]= ~r[P1] */
    int handler_rc = vdbe_op_bitnot(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Integer: {
    /* Opcode: INTEGER - Load integer constant r[P2]=P1 */
    int handler_rc = vdbe_op_integer(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Bool: {
    /* Opcode: BOOL - Load boolean constant r[P2]=P1 */
    int handler_rc = vdbe_op_bool(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Int64: {
    /* Opcode: INT64 - Load 64-bit integer r[P2]=P4 */
    int handler_rc = vdbe_op_int64(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Real: {
    /* Opcode: REAL - Load floating point r[P2]=P4 */
    int handler_rc = vdbe_op_real(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_String: {
    /* Opcode: STRING - Load string r[P2]='P4' (len=P1) */
    int handler_rc = vdbe_op_string(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Null: {
    /* Opcode: NULL - Load NULL r[P2..P3]=NULL */
    int handler_rc = vdbe_op_null(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Blob: {
    /* Opcode: BLOB - Load blob r[P2]=P4 (len=P1, subtype=P3) */
    int handler_rc = vdbe_op_blob(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Variable: {
    /* Opcode: VARIABLE - Load parameter r[P2]=parameter(P1,P4) */
    int handler_rc = vdbe_op_variable(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Move: {
    /* Opcode: MOVE - Move register r[P2@P3]=r[P1@P3] */
    int handler_rc = vdbe_op_move(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Copy: {
    /* Opcode: COPY - Copy registers r[P2@P3+1]=r[P1@P3+1] */
    int handler_rc = vdbe_op_copy(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_SCopy: {
    /* Opcode: SCOPY - Copy single register r[P2]=r[P1] */
    int handler_rc = vdbe_op_scopy(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Goto: {
    /* Opcode: GOTO - Unconditional jump to P2 */
    /* Control flow op (Phase 6): OP_Goto */
    DISPATCH();
}

Exec_OP_Jump: {
    /* Opcode: JUMP - Jump based on condition */
    /* Control flow op (Phase 6): OP_Jump */
    DISPATCH();
}

Exec_OP_If: {
    /* Opcode: IF - Jump if r[P1] is true */
    /* Control flow op (Phase 6): OP_If */
    DISPATCH();
}

Exec_OP_IfNot: {
    /* Opcode: IFNOT - Jump if r[P1] is false */
    /* Control flow op (Phase 6): OP_IfNot */
    DISPATCH();
}

Exec_OP_Once: {
    /* Opcode: ONCE - Execute body once */
    /* Control flow op (Phase 6): OP_Once */
    DISPATCH();
}

Exec_OP_Gosub: {
    /* Opcode: GOSUB - Subroutine call */
    /* Control flow op (Phase 6): OP_Gosub */
    DISPATCH();
}

Exec_OP_Return: {
    /* Opcode: RETURN - Return from subroutine */
    /* Control flow op (Phase 6): OP_Return */
    DISPATCH();
}

Exec_OP_InitCoroutine: {
    /* Opcode: INITCOROUTINE - Initialize coroutine */
    /* Control flow op (Phase 6): OP_InitCoroutine */
    DISPATCH();
}

Exec_OP_Yield: {
    /* Opcode: YIELD - Yield from coroutine */
    /* Control flow op (Phase 6): OP_Yield */
    DISPATCH();
}

Exec_OP_EndCoroutine: {
    /* Opcode: ENDCOROUTINE - End coroutine */
    /* Control flow op (Phase 6): OP_EndCoroutine */
    DISPATCH();
}

Exec_OP_ElseNotEq: {
    /* Opcode: ELSENOTEQ - Jump if comparison was not equal */
    /* Control flow op (Phase 6): OP_ElseNotEq */
    DISPATCH();
}

Exec_OP_MustBeInt: {
    /* Opcode: MUSTBEINT - Assert operand is integer */
    /* Control flow op (Phase 6): OP_MustBeInt */
    DISPATCH();
}

Exec_OP_IfPos: {
    /* Opcode: IFPOS - If r[P1]>0 then r[P1]-=P3, goto P2 */
    /* Control flow op (Phase 6): OP_IfPos */
    DISPATCH();
}

Exec_OP_IfNotZero: {
    /* Opcode: IFNOTZERO - If r[P1]!=0 then r[P1]--, goto P2 */
    /* Control flow op (Phase 6): OP_IfNotZero */
    DISPATCH();
}

Exec_OP_DecrJumpZero: {
    /* Opcode: DECRJUMPZERO - If (--r[P1])==0 goto P2 */
    /* Control flow op (Phase 6): OP_DecrJumpZero */
    DISPATCH();
}

Exec_OP_SetDiag: {
    /* Opcode: SETDIAG - Set diagnostic and jump */
    /* Control flow op (Phase 6): OP_SetDiag */
    DISPATCH();
}

Exec_OP_Halt: {
    /* Opcode: HALT - Stop execution */
    /* Control flow op (Phase 6): OP_Halt */
    DISPATCH();
}

Exec_OP_Init: {
    /* Opcode: INIT - Start execution at P2 */
    /* Control flow op (Phase 6): OP_Init */
    DISPATCH();
}

Exec_OP_Savepoint: {
    /* Opcode: SAVEPOINT - Savepoint handling */
    int p1;                         /* Value of P1 operand */
	char *zName;                    /* Name of savepoint */
	struct txn *txn = in_txn();

	if (txn == NULL) {
		assert(!box_txn());
		diag_set(ClientError, ER_NO_TRANSACTION);
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_SorterNext: {
    /* Opcode: SORTERNEXT - Sorter next row */
    /* jump */
	VdbeCursor *pC = p->apCsr[P1];
	assert(isSorter(pC));
	int res = 0;
	if (sqlVdbeSorterNext(pC, &res) != 0)
		goto abort_due_to_error;
	goto next_tail;

/* Opcode: PrevIfOpen P1 P2 P3 P4 P5
 *
 * This opcode works just like Prev except that if cursor P1 is not
 * open it behaves a no-op.
 */
EXECUTE(OP_PrevIfOpen,(P1,P2,P3,P4)):    /* jump */
	if (p->apCsr[P1] == 0) {
		DISPATCH();
    DISPATCH();
}

Exec_OP_String8: {
    /* Opcode: STRING8 - Load C string r[P2]='P4' */
    int handler_rc = vdbe_op_string8(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_SkipLoad: {
    /* Opcode: SKIPLOAD - Skip load */
    if (P1) {
		mem_set_bool(&aMem[P1], false);
    DISPATCH();
}

Exec_OP_BuiltinFunction: {
    /* Opcode: BUILTINFUNCTION - Call builtin function */
    int argc = P1;
	sql_context *pCtx;

	assert(pOp->p4type == P4_FUNCCTX);
	pCtx = pOp->p4.pCtx;

	pOut = vdbe_prepare_null_out(p, P3);
	if (pCtx->pOut != pOut)
		pCtx->pOut = pOut;

#ifdef SQL_DEBUG
	for(int i = 0; i < argc; i++) {
		assert(memIsValid(&aMem[P2 + i]));
		REGISTER_TRACE(p, P2 + i, &aMem[P2 + i]);
    DISPATCH();
}

Exec_OP_FunctionByName: {
    /* Opcode: FUNCTIONBYNAME - Call function by name */
    assert(pOp->p4type == P4_DYNAMIC);
	struct func *func = func_by_name(pOp->p4.z, strlen(pOp->p4.z));
	if (unlikely(func == NULL)) {
		diag_set(ClientError, ER_NO_SUCH_FUNCTION, pOp->p4.z);
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_AddImm: {
    /* Opcode: ADDIMM - Add immediate r[P1]=r[P1]+P2 */
    /* in1 */
	pIn1 = &aMem[P1];
	memAboutToChange(p, pIn1);
	assert(mem_is_uint(pIn1) && P2 >= 0);
	pIn1->u.u += P2;
	DISPATCH();
    DISPATCH();
}

Exec_OP_Array: {
    /* Opcode: ARRAY - Create array */
    pOut = &aMem[P2];

	uint32_t size;
	struct region *region = &fiber()->gc;
	size_t svp = region_used(region);
	char *val = mem_encode_array(&aMem[P3], P1, &size, region);
	if (val == NULL || mem_copy_array(pOut, val, size) != 0) {
		region_truncate(region, svp);
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_Map: {
    /* Opcode: MAP - Create map */
    pOut = &aMem[P2];

	uint32_t size;
	struct region *region = &fiber()->gc;
	size_t svp = region_used(region);
	char *val = mem_encode_map(&aMem[P3], P1, &size, region);
	if (val == NULL || mem_copy_map(pOut, val, size) != 0) {
		region_truncate(region, svp);
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_Getitem: {
    /* Opcode: GETITEM - Get array/map item */
    int count = P1;
	assert(count > 0);
	struct Mem *value = &aMem[P3 + count];
	if (mem_is_null(value)) {
		diag_set(ClientError, ER_SQL_EXECUTE, "Selecting is not "
			 "possible from NULL");
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_Permutation: {
    /* Opcode: PERMUTATION - Permutation */
    assert(pOp->p4type == P4_INTARRAY);
	assert(pOp->p4.ai);
	aPermute = pOp->p4.ai + 1;
	DISPATCH();
    DISPATCH();
}

Exec_OP_Compare: {
    /* Opcode: COMPARE - Compare registers */
    if ((pOp->p5 & OPFLAG_PERMUTE) == 0)
		aPermute = 0;

	int n = P3;
	assert(pOp->p4type == P4_KEYINFO);
	assert(n > 0);
	int p1 = P1;
	int p2 = P2;

	struct key_def *def = sql_key_info_to_key_def(pOp->p4.key_info);
	if (def == NULL)
		goto abort_due_to_error;
#if SQL_DEBUG
	if (aPermute) {
		int mx = 0;
		for(uint32_t k = 0; k < (uint32_t)n; k++)
			if (aPermute[k] > mx)
				mx = aPermute[k];
		assert(p1>0 && p1+mx<=(p->nMem+1 - p->nCursor)+1);
		assert(p2>0 && p2+mx<=(p->nMem+1 - p->nCursor)+1);
    DISPATCH();
}

Exec_OP_FetchByName: {
    /* Opcode: FETCHBYNAME - Fetch by name */
    struct vdbe_field_ref *ref = p->aMem[P1].u.p;
	assert(pOp->p4type == P4_DYNAMIC);
	uint32_t id;
	if (ref->format != NULL) {
		const char *name = pOp->p4.z;
		uint32_t len = strlen(name);
		uint32_t hash = field_name_hash(name, len);
		struct tuple_dictionary *dict = ref->format->dict;
		if (tuple_fieldno_by_name(dict, name, len, hash, &id) != 0) {
			diag_set(ClientError, ER_SQL_CANT_RESOLVE_FIELD, name);
			goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_Fetch: {
    /* Opcode: FETCH - Fetch record */
    struct vdbe_field_ref *ref = p->aMem[P1].u.p;
	struct Mem *res = vdbe_prepare_null_out(p, P3);
	if (vdbe_field_ref_fetch(ref, P2, res) != 0)
		goto abort_due_to_error;
	REGISTER_TRACE(p, P3, res);
	DISPATCH();
    DISPATCH();
}

Exec_OP_Count: {
    /* Opcode: COUNT - Count records */
    /* out2 */
	i64 nEntry;
	BtCursor *pCrsr;

	assert(p->apCsr[P1]->eCurType == CURTYPE_TARANTOOL);
	pCrsr = p->apCsr[P1]->uc.pCursor;
	assert(pCrsr);
	if (pCrsr->curFlags & BTCF_TaCursor) {
		nEntry = tarantoolsqlCount(pCrsr);
    DISPATCH();
}

Exec_OP_CreateForeignKey: {
    /* Opcode: CREATEFOREIGNKEY - Create foreign key constraint */
    assert(P1 >= 0);
	struct Mem *mems = &aMem[P1];
	assert(mem_is_uint(&mems[0]) && mem_is_uint(&mems[1]));
	uint32_t child_id = mems[0].u.u;
	uint32_t parent_id = mems[1].u.u;
	const char *name = pOp->p4.z;
	const char *mapping = NULL;
	uint32_t child_fieldno = 0;
	uint32_t parent_fieldno = 0;
	if (mem_is_uint(&mems[2])) {
		assert(mem_is_uint(&mems[3]));
		child_fieldno = mems[2].u.u;
		parent_fieldno = mems[3].u.u;
    DISPATCH();
}

Exec_OP_CreateCheck: {
    /* Opcode: CREATECHECK - Create check constraint */
    assert(P1 >= 0 && P2 >= 0 && P3 >= 0);
	uint32_t space_id = aMem[P1].u.u;
	uint32_t func_id = aMem[P2].u.u;
	const char *name = pOp->p4.z;
	bool is_field_ck = pOp->p5 != 0;
	uint32_t fieldno = P3;
	if (sql_check_create(name, space_id, func_id, fieldno,
			     is_field_ck) != 0)
		goto abort_due_to_error;
	if (p->nChange == 0)
		p->nChange = 1;
	DISPATCH();
    DISPATCH();
}

Exec_OP_DropTupleForeignKey: {
    /* Opcode: DROPTUPLEFOREIGNKEY - Drop tuple foreign key */
    assert(P1 >= 0 && pOp->p4.z != NULL);
	if (sql_tuple_foreign_key_drop(P1, pOp->p4.z) != 0)
		goto abort_due_to_error;
	assert(p->nChange == 0);
	p->nChange = 1;
	DISPATCH();
    DISPATCH();
}

Exec_OP_DropTupleCheck: {
    /* Opcode: DROPTUPLECHECK - Drop tuple check */
    assert(P1 >= 0 && pOp->p4.z != NULL);
	if (sql_tuple_check_drop(P1, pOp->p4.z) != 0)
		goto abort_due_to_error;
	assert(p->nChange == 0);
	p->nChange = 1;
	DISPATCH();
    DISPATCH();
}

Exec_OP_DropFieldForeignKey: {
    /* Opcode: DROPFIELDFOREIGNKEY - Drop field foreign key */
    assert(P1 >= 0 && pOp->p4.z != NULL);
	if (sql_field_foreign_key_drop(P1, P3, pOp->p4.z) != 0)
		goto abort_due_to_error;
	assert(p->nChange == 0);
	p->nChange = 1;
	DISPATCH();
    DISPATCH();
}

Exec_OP_DropFieldCheck: {
    /* Opcode: DROPFIELDCHECK - Drop field check */
    assert(P1 >= 0 && pOp->p4.z != NULL);
	if (sql_field_check_drop(P1, P3, pOp->p4.z) != 0)
		goto abort_due_to_error;
	assert(p->nChange == 0);
	p->nChange = 1;
	DISPATCH();
    DISPATCH();
}

Exec_OP_AddFuncDefault: {
    /* Opcode: ADDFUNCDEFAULT - Add function as default */
    assert(aMem[P1].type == MEM_TYPE_UINT);
	uint32_t space_id = aMem[P1].u.u;
	uint32_t fieldno = P3;
	assert(aMem[P2].type == MEM_TYPE_UINT);
	uint32_t func_id = aMem[P2].u.u;
	if (sql_add_default(space_id, fieldno, func_id) != 0)
		goto abort_due_to_error;
	DISPATCH();
    DISPATCH();
}

Exec_OP_CheckViewReferences: {
    /* Opcode: CHECKVIEWREFERENCES - Check view references */
    assert(P1 > 0);
	pIn1 = &aMem[P1];
	uint64_t space_id = pIn1->u.u;
	assert(space_id <= INT32_MAX);
	struct space *space = space_by_id(space_id);
	assert(space != NULL);
	if (space->def->view_ref_count > 0) {
		diag_set(ClientError, ER_DROP_SPACE, space->def->name,
			 "other views depend on this space");
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_TransactionBegin: {
    /* Opcode: TRANSACTIONBEGIN - Begin transaction */
    if (in_txn()) {
		diag_set(ClientError, ER_ACTIVE_TRANSACTION);
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_TransactionCommit: {
    /* Opcode: TRANSACTIONCOMMIT - Commit transaction */
    struct txn *txn = in_txn();
	if (txn != NULL) {
		if (txn_commit(txn) != 0)
			goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_TransactionRollback: {
    /* Opcode: TRANSACTIONROLLBACK - Rollback transaction */
    if (box_txn()) {
		if (box_txn_rollback() != 0)
			goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_TTransaction: {
    /* Opcode: TTRANSACTION - Tarantool transaction */
    if (!box_txn()) {
		if (txn_begin() == NULL)
			goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_IteratorOpen: {
    /* Opcode: ITERATOROPEN - Open iterator */
    struct VdbeCursor *cur = p->apCsr[P1];
	if (box_schema_version() != p->schema_ver &&
	    (pOp->p5 & OPFLAG_SYSTEMSP) == 0) {
		p->expired = 1;
		diag_set(ClientError, ER_SQL_EXECUTE, "schema version has "\
			 "changed: need to re-compile SQL statement");
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_OpenSpace: {
    /* Opcode: OPENSPACE - Open space cursor */
    assert(P1 >= 0 && P1 > 0);
	struct space *space = space_by_id(P2);
	assert(space != NULL);
	mem_set_ptr(&aMem[P1], space);
	DISPATCH();
    DISPATCH();
}

Exec_OP_OpenTEphemeral: {
    /* Opcode: OPENTEPHEMERAL - Open temporary table */
    assert(P1 >= 0);

	assert(pOp->p4type == P4_DYNAMIC || pOp->p4type == P4_STATIC);
	struct sql_space_info *info = pOp->p4.space_info;
	assert(info != NULL);
	struct space *space = sql_ephemeral_space_new(info);

	if (space == NULL)
		goto abort_due_to_error;
	mem_set_ptr(&aMem[P1], space);
	DISPATCH();
    DISPATCH();
}

Exec_OP_SorterOpen: {
    /* Opcode: SORTEROPEN - Open sorter */
    VdbeCursor *pCx;

	assert(P1 >= 0);
	assert(P2 >= 0);
	struct key_def *def = sql_key_info_to_key_def(pOp->p4.key_info);
	if (def == NULL)
		goto abort_due_to_error;
	pCx = allocateCursor(p, P1, P2, CURTYPE_SORTER);
	if (pCx == NULL)
		goto abort_due_to_error;
	pCx->key_def = def;
	if (sqlVdbeSorterInit(pCx) != 0)
		goto abort_due_to_error;
	DISPATCH();
    DISPATCH();
}

Exec_OP_SequenceTest: {
    /* Opcode: SEQUENCETEST - Test sequence counter */
    VdbeCursor *pC;
	assert(P1 >= 0 && P1 < p->nCursor);
	pC = p->apCsr[P1];
	assert(isSorter(pC));
	if ((pC->seqCount++) == 0)
		JUMP_P2();

	DISPATCH();
    DISPATCH();
}

Exec_OP_OpenPseudo: {
    /* Opcode: OPENPSEUDO - Open pseudo-cursor */
    VdbeCursor *pCx;

	assert(P1 >= 0);
	assert(P3 >= 0);
	pCx = allocateCursor(p, P1, P3, CURTYPE_PSEUDO);
	if (pCx == NULL)
		goto abort_due_to_error;
	pCx->nullRow = 1;
	pCx->uc.pseudoTableReg = P2;
	assert(pOp->p5==0);
	DISPATCH();
    DISPATCH();
}

Exec_OP_Close: {
    /* Opcode: CLOSE - Close cursor */
    assert(P1 >= 0 && P1 < p->nCursor);
	sqlVdbeFreeCursor(p->apCsr[P1]);
	p->apCsr[P1] = 0;
	DISPATCH();
    DISPATCH();
}

Exec_OP_Sequence: {
    /* Opcode: SEQUENCE - Get sequence value */
    /* out2 */
	assert(P1>=0 && P1<p->nCursor);
	assert(p->apCsr[P1]!=0);
	pOut = vdbe_prepare_null_out(p, P2);
	int64_t seq_val = p->apCsr[P1]->seqCount++;
	mem_set_uint(pOut, seq_val);
	DISPATCH();
    DISPATCH();
}

Exec_OP_NextSystemSpaceId: {
    /* Opcode: NEXTSYSTEMSPACEID - Get next system space ID */
    assert(P1 >= 0 && P3 >= 0);
	uint32_t space_id = P1;
	assert(space_id == BOX_SEQUENCE_ID || space_id == BOX_FUNC_ID);
	struct Mem *res = &p->aMem[P2];
	char key[1];
	struct tuple *tuple;
	char *key_end = mp_encode_array(key, 0);
	assert(key_end - key == 1);
	if (box_index_max(space_id, 0, key, key_end, &tuple) != 0)
		goto abort_due_to_error;
	if (tuple == NULL) {
		mem_set_uint(res, 1);
		DISPATCH();
    DISPATCH();
}

Exec_OP_NextIdEphemeral: {
    /* Opcode: NEXTIDEPHEMERAL - Get next ephemeral ID */
    struct space *space = (struct space*)p->aMem[P1].u.p;
	assert(space->def->id == 0);
	uint64_t rowid;
	if (space->vtab->ephemeral_rowid_next(space, &rowid) != 0)
		goto abort_due_to_error;
	/*
	 * FIXME: since memory cell can comprise only 32-bit
	 * integer, make sure it can fit in. This check should
	 * be removed when memory cell is extended with unsigned
	 * 64-bit integer.
	 */
	if (rowid > INT32_MAX) {
		diag_set(ClientError, ER_ROWID_OVERFLOW);
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_FCopy: {
    /* Opcode: FCOPY - Frame copy */
    /* out2 */
	VdbeFrame *pFrame;
	Mem *pIn1, *pOut;
	if (p->pFrame && ((P3 & OPFLAG_SAME_FRAME) == 0)) {
		for(pFrame=p->pFrame; pFrame->pParent; pFrame=pFrame->pParent);
		pIn1 = &pFrame->aMem[P1];
    DISPATCH();
}

Exec_OP_ResetCount: {
    /* Opcode: RESETCOUNT - Reset counter */
    sqlVdbeSetChanges(p->nChange);
	p->nChange = 0;
	p->ignoreRaised = 0;
	DISPATCH();
    DISPATCH();
}

Exec_OP_SorterCompare: {
    /* Opcode: SORTERCOMPARE - Sorter comparison */
    VdbeCursor *pC;
	int res;
	int nKeyCol;

	pC = p->apCsr[P1];
	assert(isSorter(pC));
	assert(pOp->p4type == P4_INT32);
	pIn3 = &aMem[P3];
	nKeyCol = pOp->p4.i;
	if (sqlVdbeSorterCompare(pC, pIn3, nKeyCol, &res) != 0)
		goto abort_due_to_error;
	if (res) JUMP_P2();
	DISPATCH();
    DISPATCH();
}

Exec_OP_SorterData: {
    /* Opcode: SORTERDATA - Get sorter data */
    VdbeCursor *pC;

	pOut = vdbe_prepare_null_out(p, P2);
	pC = p->apCsr[P1];
	assert(isSorter(pC));
	if (sqlVdbeSorterRowkey(pC, pOut) != 0)
		goto abort_due_to_error;
	assert(mem_is_bin(pOut));
	assert(P1 >= 0 && P1 < p->nCursor);
	p->apCsr[P3]->cacheStatus = CACHE_STALE;
	DISPATCH();
    DISPATCH();
}

Exec_OP_NullRow: {
    /* Opcode: NULLROW - Null row marker */
    VdbeCursor *pC;

	assert(P1 >= 0 && P1 < p->nCursor);
	pC = p->apCsr[P1];
	assert(pC!=0);
	pC->nullRow = 1;
	pC->cacheStatus = CACHE_STALE;
	if (pC->eCurType==CURTYPE_TARANTOOL) {
		assert(pC->uc.pCursor!=0);
		sql_cursor_cleanup(pC->uc.pCursor);
    DISPATCH();
}

Exec_OP_SorterInsert: {
    /* Opcode: SORTERINSERT - Insert into sorter */
    /* in2 */
	assert(P1 >= 0 && P1 < p->nCursor);
	struct VdbeCursor *cursor = p->apCsr[P1];
	assert(cursor != NULL);
	assert(isSorter(cursor));
	pIn2 = &aMem[P2];
	assert(mem_is_bin(pIn2));
	if (sqlVdbeSorterWrite(cursor, pIn2) != 0)
		goto abort_due_to_error;
	DISPATCH();
    DISPATCH();
}

Exec_OP_Clear: {
    /* Opcode: CLEAR - Clear space */
    assert(P1 > 0);
	uint32_t space_id = P1;
	struct space *space = space_by_id(space_id);
	assert(space != NULL);
	if (P2 > 0) {
		if (box_truncate(space_id) != 0)
			goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_ResetSorter: {
    /* Opcode: RESETSORTER - Reset sorter */
    VdbeCursor *pC;

	assert(P1 >= 0 && P1 < p->nCursor);
	pC = p->apCsr[P1];
	assert(pC != 0);
	if (isSorter(pC)) {
		sqlVdbeSorterReset(pC->uc.pSorter);
    DISPATCH();
}

Exec_OP_RenameTable: {
    /* Opcode: RENAMETABLE - Rename table */
    uint32_t space_id;
	struct space *space;
	char *zOldTableName;
	const char *zNewTableName;

	space_id = P1;
	space = space_by_id(space_id);
	assert(space);
	/* Rename space op doesn't change triggers. */
	struct sql_trigger *triggers = space->sql_triggers;
	assert(space->def->name != NULL);
	zNewTableName = pOp->p4.z;
	zOldTableName = sql_xstrdup(space_name(space));
	if (sql_rename_table(space_id, zNewTableName) != 0)
		goto abort_due_to_error;
	/*
	 * Rebuild 'CREATE TRIGGER' expressions of all triggers
	 * created on this table. Sure, this action is not atomic
	 * due to lack of transactional DDL, but just do the best
	 * effort.
	 */
	for (struct sql_trigger *trigger = triggers; trigger != NULL; ) {
		/* Store pointer as trigger will be destructed. */
		struct sql_trigger *next_trigger = trigger->next;
		/*
		 * FIXME: In the case of error, part of triggers
		 * would have invalid space name in tuple so can
		 * not been persisted. Server could be restarted.
		 * In this case, rename table back and try again.
		 */
		if (tarantoolsqlRenameTrigger(trigger->zName, zOldTableName,
					      zNewTableName) != 0)
			goto abort_due_to_error;
		trigger = next_trigger;
    DISPATCH();
}

Exec_OP_LoadAnalysis: {
    /* Opcode: LOADANALYSIS - Load analysis */
    assert(P1 == 0);
	/* TODO: Enable analysis. */
	/*
	if (sql_analysis_load(db) != 0)
		goto abort_due_to_error;
	*/
	DISPATCH();
    DISPATCH();
}

Exec_OP_Param: {
    /* Opcode: PARAM - Load parameter */
    /* out2 */
	VdbeFrame *pFrame;
	Mem *pIn;
	pOut = vdbe_prepare_null_out(p, P2);
	pFrame = p->pFrame;
	pIn = &pFrame->aMem[P1 + pFrame->aOp[pFrame->pc].p1];
	mem_copy_as_ephemeral(pOut, pIn);
	DISPATCH();
    DISPATCH();
}

Exec_OP_OffsetLimit: {
    /* Opcode: OFFSETLIMIT - Calculate offset limit (r[P2]=r[P1]+r[P3]) */
    int handler_rc = vdbe_op_offsetlimit(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        /* Special return value handling (jump or SQL_ROW) */
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        /* For comparison ops, jump to P2 */
        JUMP_P2();
    }
    DISPATCH();
}

Exec_OP_Expire: {
    /* Opcode: EXPIRE - Expire entry */
    if (!P1) {
		sqlExpirePreparedStatements();
    DISPATCH();
}

Exec_OP_GenSpaceid: {
    /* Opcode: GENSPACEID - Generate space ID */
    assert(P1 > 0);
	pOut = vdbe_prepare_null_out(p, P1);
	uint32_t u;
	if (box_generate_space_id(&u, false) != 0)
		goto abort_due_to_error;
	mem_set_uint(pOut, u);
	DISPATCH();
    DISPATCH();
}

Exec_OP_SetSession: {
    /* Opcode: SETSESSION - Set session */
    assert(pOp->p4type == P4_DYNAMIC);
	const char *setting_name = pOp->p4.z;
	int sid = session_setting_find(setting_name);
	if (sid < 0) {
		diag_set(ClientError, ER_NO_SUCH_SESSION_SETTING, setting_name);
		goto abort_due_to_error;
    DISPATCH();
}

Exec_OP_ShowCreateTable: {
    /* Opcode: SHOWCREATETABLE - Show create table */
    struct Mem *ret = &aMem[P2];
	struct Mem *err = &aMem[P2 + 1];
	sql_show_create_table(aMem[P1].u.i, ret, err);
	DISPATCH();
    DISPATCH();
}

Exec_OP_Noop: {
    /* Opcode: NOOP - No operation */
    DISPATCH();
    DISPATCH();
}

Exec_OP_Explain: {
    /* Opcode: EXPLAIN - Explain query */
    DISPATCH();
    DISPATCH();
}

Exec_OP_IsNull: {
    /* Opcode: ISNULL - Test for NULL */
    /* same as TK_ISNULL, jump, in1 */
	pIn1 = &aMem[P1];
	if (mem_is_null(pIn1))
		JUMP_P2();
	DISPATCH();
    DISPATCH();
}

Exec_OP_NotNull: {
    /* Opcode: NOTNULL - Test for not NULL */
    /* same as TK_NOTNULL, jump, in1 */
	pIn1 = &aMem[P1];
	if (!mem_is_null(pIn1)) {
		JUMP_P2();
    DISPATCH();
}

Exec_OP_Decimal: {
    /* Opcode: DECIMAL - Load decimal r[P2]=P4 */
    /* same as TK_DECIMAL, out2 */
	pOut = vdbe_prepare_null_out(p, P2);
	mem_set_dec(pOut, pOp->p4.dec);
	DISPATCH();
    DISPATCH();
}

Exec_OP_Program: {
    /* Opcode: PROGRAM - Execute program */
    /* jump */
	int nMem;               /* Number of memory registers for sub-program */
	int nByte;              /* Bytes of runtime space required for sub-program */
	Mem *pRt;               /* Register to allocate runtime space */
	Mem *pMem;              /* Used to iterate through memory cells */
	Mem *pEnd;              /* Last memory cell in new array */
	VdbeFrame *pFrame;      /* New vdbe frame to execute in */
	SubProgram *pProgram;   /* Sub-program to execute */
	void *t;                /* Token identifying trigger */

	pProgram = pOp->p4.pProgram;
	pRt = &aMem[P3];
	assert(pProgram->nOp>0);

	/* If the p5 flag is clear, then recursive invocation of triggers is
	 * disabled for backwards compatibility (p5 is set if this sub-program
	 * is really a trigger, not a foreign key action, and the setting
	 * 'recursive_triggers' is not set).
	 *
	 * It is recursive invocation of triggers, at the SQL level, that is
	 * disabled. In some cases a single trigger may generate more than one
	 * SubProgram (if the trigger may be executed with more than one different
	 * ON CONFLICT algorithm). SubProgram structures associated with a
	 * single trigger all have the same value for the SubProgram.token
	 * variable.
	 */
	if (pOp->p5) {
		t = pProgram->token;
		for(pFrame=p->pFrame; pFrame && pFrame->token!=t; pFrame=pFrame->pParent);
		if (pFrame) {
			DISPATCH();
    DISPATCH();
}

Exec_OP_Sort: {
    /* Opcode: SORT - Sort records */
    /* jump */
#ifdef SQL_TEST
	sql_sort_count++;
	sql_search_count--;
#endif
	/* Fall through into OP_Rewind */
#ifndef SQL_USE_GOTO
	FALLTHROUGH;
#endif /* SQL_USE_GOTO */
    DISPATCH();
}

Exec_OP_SorterSort: {
    /* Opcode: SORTERSORT - Sort sorter */
    /* jump */
#ifdef SQL_TEST
	sql_sort_count++;
	sql_search_count--;
#endif
	/* Fall through into OP_Rewind */
#ifndef SQL_USE_GOTO
	FALLTHROUGH;
#endif /* SQL_USE_GOTO */
    DISPATCH();
}

Exec_OP_ShiftLeft: {
    /* Opcode: SHIFTLEFT - Bitwise left shift */
    /* same as TK_LSHIFT, in1, in2, out3 */
	pIn1 = &aMem[P1];
	pIn2 = &aMem[P2];
	pOut = &aMem[P3];
	if (mem_shift_left(pIn2, pIn1, pOut) != 0)
		goto abort_due_to_error;
	assert(pOut->type == MEM_TYPE_UINT || pOut->type == MEM_TYPE_NULL);
	DISPATCH();
    DISPATCH();
}

Exec_OP_ShiftRight: {
    /* Opcode: SHIFTRIGHT - Bitwise right shift */
    /* same as TK_RSHIFT, in1, in2, out3 */
	pIn1 = &aMem[P1];
	pIn2 = &aMem[P2];
	pOut = &aMem[P3];
	if (mem_shift_right(pIn2, pIn1, pOut) != 0)
		goto abort_due_to_error;
	assert(pOut->type == MEM_TYPE_UINT || pOut->type == MEM_TYPE_NULL);
	DISPATCH();
    DISPATCH();
}

#else /* SQL_USE_GOTO */
/* Generated dispatch loop - switch statement version */
/* This is included as part of sqlVdbeExec() when SQL_USE_GOTO is not defined */

start_eval: switch (pOp->opcode) {

case OP_Concat: {
    /* CONCAT - String concatenation r[P3]=r[P1]+r[P2] */
    int handler_rc = vdbe_op_concat(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Cast: {
    /* CAST - Explicit type cast r[P1] */
    int handler_rc = vdbe_op_cast(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_ApplyType: {
    /* APPLYTYPE - Apply type checking r[P1@P2] */
    int handler_rc = vdbe_op_applytype(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_MakeRecord: {
    /* MAKERECORD - Create msgpack record r[P3]=mkrec(r[P1@P2]) */
    int handler_rc = vdbe_op_makerecord(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_AggStep: {
    /* AGGSTEP - Aggregate step accum=r[P3] step(r[P2@P1]) */
    int handler_rc = vdbe_op_aggstep(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_AggFinal: {
    /* AGGFINAL - Aggregate finalize accum=r[P1] */
    int handler_rc = vdbe_op_aggfinal(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_ResultRow: {
    /* RESULTROW - Return result row output=r[P1@P2] */
    int handler_rc = vdbe_op_resultrow(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Column: {
    /* COLUMN - Extract column from cursor r[P3]=PX */
    int handler_rc = vdbe_op_column(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_RowData: {
    /* ROWDATA - Get complete row data r[P2]=data */
    int handler_rc = vdbe_op_rowdata(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Rewind: {
    /* REWIND - Rewind cursor to first row */
    int handler_rc = vdbe_op_rewind(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Last: {
    /* LAST - Position cursor to last row */
    int handler_rc = vdbe_op_last(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Next: {
    /* NEXT - Advance cursor to next row */
    int handler_rc = vdbe_op_next(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_NextIfOpen: {
    /* NEXTIFOPEN - Advance cursor if open */
    int handler_rc = vdbe_op_nextifopen(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Prev: {
    /* PREV - Move cursor to previous row */
    int handler_rc = vdbe_op_prev(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_PrevIfOpen: {
    /* PREVIFOPEN - Move cursor to previous row if open */
    int handler_rc = vdbe_op_previfopen(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_SeekLE: {
    /* SEEKLE - Seek to LE key position key=r[P3@P4] */
    int handler_rc = vdbe_op_seekle(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_SeekGT: {
    /* SEEKGT - Seek to GT key position key=r[P3@P4] */
    int handler_rc = vdbe_op_seekgt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_SeekGE: {
    /* SEEKGE - Seek to GE key position key=r[P3@P4] */
    int handler_rc = vdbe_op_seekge(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_SeekLT: {
    /* SEEKLT - Seek to LT key position key=r[P3@P4] */
    int handler_rc = vdbe_op_seeklt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_IdxGE: {
    /* IDXGE - Index comparison GE key=r[P3@P4] */
    int handler_rc = vdbe_op_idxge(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_IdxGT: {
    /* IDXGT - Index comparison GT key=r[P3@P4] */
    int handler_rc = vdbe_op_idxgt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_IdxLE: {
    /* IDXLE - Index comparison LE key=r[P3@P4] */
    int handler_rc = vdbe_op_idxle(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_IdxLT: {
    /* IDXLT - Index comparison LT key=r[P3@P4] */
    int handler_rc = vdbe_op_idxlt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Found: {
    /* FOUND - Index lookup - exact match key=r[P3@P4] */
    int handler_rc = vdbe_op_found(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_NotFound: {
    /* NOTFOUND - Index lookup - not found key=r[P3@P4] */
    int handler_rc = vdbe_op_notfound(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_NoConflict: {
    /* NOCONFLICT - Index conflict check key=r[P3@P4] */
    int handler_rc = vdbe_op_noconflict(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_IdxInsert: {
    /* IDXINSERT - Insert index entry key=r[P1] */
    int handler_rc = vdbe_op_idxinsert(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_IdxReplace: {
    /* IDXREPLACE - Replace index entry key=r[P1] */
    int handler_rc = vdbe_op_idxreplace(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Delete: {
    /* DELETE - Delete record from table key=r[P1] */
    int handler_rc = vdbe_op_delete(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Update: {
    /* UPDATE - Update record in table key=r[P1] */
    int handler_rc = vdbe_op_update(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_SInsert: {
    /* SINSERT - Insert into system space space id = P1, key = r[P2] */
    int handler_rc = vdbe_op_sinsert(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_SDelete: {
    /* SDELETE - Delete from system space space id = P1, key = r[P2] */
    int handler_rc = vdbe_op_sdelete(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_IdxDelete: {
    /* IDXDELETE - Delete index entry key=r[P2@P3] */
    int handler_rc = vdbe_op_idxdelete(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Add: {
    /* ADD - Addition r[P3]=r[P1]+r[P2] */
    int handler_rc = vdbe_op_add(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Subtract: {
    /* SUBTRACT - Subtraction r[P3]=r[P2]-r[P1] */
    int handler_rc = vdbe_op_subtract(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Multiply: {
    /* MULTIPLY - Multiplication r[P3]=r[P1]*r[P2] */
    int handler_rc = vdbe_op_multiply(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Divide: {
    /* DIVIDE - Division r[P3]=r[P2]/r[P1] */
    int handler_rc = vdbe_op_divide(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Remainder: {
    /* REMAINDER - Modulo r[P3]=r[P2]%r[P1] */
    int handler_rc = vdbe_op_remainder(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Eq: {
    /* EQ - Equality comparison if r[P3]==r[P1] goto P2 */
    int handler_rc = vdbe_op_eq(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Ne: {
    /* NE - Not equal comparison if r[P3]!=r[P1] goto P2 */
    int handler_rc = vdbe_op_ne(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Lt: {
    /* LT - Less than comparison if r[P3]<r[P1] goto P2 */
    int handler_rc = vdbe_op_lt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Le: {
    /* LE - Less or equal comparison if r[P3]<=r[P1] goto P2 */
    int handler_rc = vdbe_op_le(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Gt: {
    /* GT - Greater than comparison if r[P3]>r[P1] goto P2 */
    int handler_rc = vdbe_op_gt(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Ge: {
    /* GE - Greater or equal comparison if r[P3]>=r[P1] goto P2 */
    int handler_rc = vdbe_op_ge(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_And: {
    /* AND - Logical AND r[P3]=(r[P1] && r[P2]) */
    int handler_rc = vdbe_op_and(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Or: {
    /* OR - Logical OR r[P3]=(r[P1] || r[P2]) */
    int handler_rc = vdbe_op_or(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Not: {
    /* NOT - Logical NOT r[P2]= !r[P1] */
    int handler_rc = vdbe_op_not(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_BitAnd: {
    /* BITAND - Bitwise AND r[P3]=r[P1]&r[P2] */
    int handler_rc = vdbe_op_bitand(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_BitOr: {
    /* BITOR - Bitwise OR r[P3]=r[P1]|r[P2] */
    int handler_rc = vdbe_op_bitor(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_BitNot: {
    /* BITNOT - Bitwise NOT r[P1]= ~r[P1] */
    int handler_rc = vdbe_op_bitnot(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Integer: {
    /* INTEGER - Load integer constant r[P2]=P1 */
    int handler_rc = vdbe_op_integer(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Bool: {
    /* BOOL - Load boolean constant r[P2]=P1 */
    int handler_rc = vdbe_op_bool(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Int64: {
    /* INT64 - Load 64-bit integer r[P2]=P4 */
    int handler_rc = vdbe_op_int64(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Real: {
    /* REAL - Load floating point r[P2]=P4 */
    int handler_rc = vdbe_op_real(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_String: {
    /* STRING - Load string r[P2]='P4' (len=P1) */
    int handler_rc = vdbe_op_string(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Null: {
    /* NULL - Load NULL r[P2..P3]=NULL */
    int handler_rc = vdbe_op_null(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Blob: {
    /* BLOB - Load blob r[P2]=P4 (len=P1, subtype=P3) */
    int handler_rc = vdbe_op_blob(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Variable: {
    /* VARIABLE - Load parameter r[P2]=parameter(P1,P4) */
    int handler_rc = vdbe_op_variable(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Move: {
    /* MOVE - Move register r[P2@P3]=r[P1@P3] */
    int handler_rc = vdbe_op_move(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Copy: {
    /* COPY - Copy registers r[P2@P3+1]=r[P1@P3+1] */
    int handler_rc = vdbe_op_copy(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_SCopy: {
    /* SCOPY - Copy single register r[P2]=r[P1] */
    int handler_rc = vdbe_op_scopy(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Goto: {
    /* GOTO - Unconditional jump to P2 */
    /* Control flow op (Phase 6): OP_Goto */
    break;
}

case OP_Jump: {
    /* JUMP - Jump based on condition */
    /* Control flow op (Phase 6): OP_Jump */
    break;
}

case OP_If: {
    /* IF - Jump if r[P1] is true */
    /* Control flow op (Phase 6): OP_If */
    break;
}

case OP_IfNot: {
    /* IFNOT - Jump if r[P1] is false */
    /* Control flow op (Phase 6): OP_IfNot */
    break;
}

case OP_Once: {
    /* ONCE - Execute body once */
    /* Control flow op (Phase 6): OP_Once */
    break;
}

case OP_Gosub: {
    /* GOSUB - Subroutine call */
    /* Control flow op (Phase 6): OP_Gosub */
    break;
}

case OP_Return: {
    /* RETURN - Return from subroutine */
    /* Control flow op (Phase 6): OP_Return */
    break;
}

case OP_InitCoroutine: {
    /* INITCOROUTINE - Initialize coroutine */
    /* Control flow op (Phase 6): OP_InitCoroutine */
    break;
}

case OP_Yield: {
    /* YIELD - Yield from coroutine */
    /* Control flow op (Phase 6): OP_Yield */
    break;
}

case OP_EndCoroutine: {
    /* ENDCOROUTINE - End coroutine */
    /* Control flow op (Phase 6): OP_EndCoroutine */
    break;
}

case OP_ElseNotEq: {
    /* ELSENOTEQ - Jump if comparison was not equal */
    /* Control flow op (Phase 6): OP_ElseNotEq */
    break;
}

case OP_MustBeInt: {
    /* MUSTBEINT - Assert operand is integer */
    /* Control flow op (Phase 6): OP_MustBeInt */
    break;
}

case OP_IfPos: {
    /* IFPOS - If r[P1]>0 then r[P1]-=P3, goto P2 */
    /* Control flow op (Phase 6): OP_IfPos */
    break;
}

case OP_IfNotZero: {
    /* IFNOTZERO - If r[P1]!=0 then r[P1]--, goto P2 */
    /* Control flow op (Phase 6): OP_IfNotZero */
    break;
}

case OP_DecrJumpZero: {
    /* DECRJUMPZERO - If (--r[P1])==0 goto P2 */
    /* Control flow op (Phase 6): OP_DecrJumpZero */
    break;
}

case OP_SetDiag: {
    /* SETDIAG - Set diagnostic and jump */
    /* Control flow op (Phase 6): OP_SetDiag */
    break;
}

case OP_Halt: {
    /* HALT - Stop execution */
    /* Control flow op (Phase 6): OP_Halt */
    break;
}

case OP_Init: {
    /* INIT - Start execution at P2 */
    /* Control flow op (Phase 6): OP_Init */
    break;
}

case OP_Savepoint: {
    /* SAVEPOINT - Savepoint handling */
    int p1;                         /* Value of P1 operand */
	char *zName;                    /* Name of savepoint */
	struct txn *txn = in_txn();

	if (txn == NULL) {
		assert(!box_txn());
		diag_set(ClientError, ER_NO_TRANSACTION);
		goto abort_due_to_error;
    break;
}

case OP_SorterNext: {
    /* SORTERNEXT - Sorter next row */
    /* jump */
	VdbeCursor *pC = p->apCsr[P1];
	assert(isSorter(pC));
	int res = 0;
	if (sqlVdbeSorterNext(pC, &res) != 0)
		goto abort_due_to_error;
	goto next_tail;

/* Opcode: PrevIfOpen P1 P2 P3 P4 P5
 *
 * This opcode works just like Prev except that if cursor P1 is not
 * open it behaves a no-op.
 */
EXECUTE(OP_PrevIfOpen,(P1,P2,P3,P4)):    /* jump */
	if (p->apCsr[P1] == 0) {
		DISPATCH();
    break;
}

case OP_String8: {
    /* STRING8 - Load C string r[P2]='P4' */
    int handler_rc = vdbe_op_string8(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_SkipLoad: {
    /* SKIPLOAD - Skip load */
    if (P1) {
		mem_set_bool(&aMem[P1], false);
    break;
}

case OP_BuiltinFunction: {
    /* BUILTINFUNCTION - Call builtin function */
    int argc = P1;
	sql_context *pCtx;

	assert(pOp->p4type == P4_FUNCCTX);
	pCtx = pOp->p4.pCtx;

	pOut = vdbe_prepare_null_out(p, P3);
	if (pCtx->pOut != pOut)
		pCtx->pOut = pOut;

#ifdef SQL_DEBUG
	for(int i = 0; i < argc; i++) {
		assert(memIsValid(&aMem[P2 + i]));
		REGISTER_TRACE(p, P2 + i, &aMem[P2 + i]);
    break;
}

case OP_FunctionByName: {
    /* FUNCTIONBYNAME - Call function by name */
    assert(pOp->p4type == P4_DYNAMIC);
	struct func *func = func_by_name(pOp->p4.z, strlen(pOp->p4.z));
	if (unlikely(func == NULL)) {
		diag_set(ClientError, ER_NO_SUCH_FUNCTION, pOp->p4.z);
		goto abort_due_to_error;
    break;
}

case OP_AddImm: {
    /* ADDIMM - Add immediate r[P1]=r[P1]+P2 */
    /* in1 */
	pIn1 = &aMem[P1];
	memAboutToChange(p, pIn1);
	assert(mem_is_uint(pIn1) && P2 >= 0);
	pIn1->u.u += P2;
	DISPATCH();
    break;
}

case OP_Array: {
    /* ARRAY - Create array */
    pOut = &aMem[P2];

	uint32_t size;
	struct region *region = &fiber()->gc;
	size_t svp = region_used(region);
	char *val = mem_encode_array(&aMem[P3], P1, &size, region);
	if (val == NULL || mem_copy_array(pOut, val, size) != 0) {
		region_truncate(region, svp);
		goto abort_due_to_error;
    break;
}

case OP_Map: {
    /* MAP - Create map */
    pOut = &aMem[P2];

	uint32_t size;
	struct region *region = &fiber()->gc;
	size_t svp = region_used(region);
	char *val = mem_encode_map(&aMem[P3], P1, &size, region);
	if (val == NULL || mem_copy_map(pOut, val, size) != 0) {
		region_truncate(region, svp);
		goto abort_due_to_error;
    break;
}

case OP_Getitem: {
    /* GETITEM - Get array/map item */
    int count = P1;
	assert(count > 0);
	struct Mem *value = &aMem[P3 + count];
	if (mem_is_null(value)) {
		diag_set(ClientError, ER_SQL_EXECUTE, "Selecting is not "
			 "possible from NULL");
		goto abort_due_to_error;
    break;
}

case OP_Permutation: {
    /* PERMUTATION - Permutation */
    assert(pOp->p4type == P4_INTARRAY);
	assert(pOp->p4.ai);
	aPermute = pOp->p4.ai + 1;
	DISPATCH();
    break;
}

case OP_Compare: {
    /* COMPARE - Compare registers */
    if ((pOp->p5 & OPFLAG_PERMUTE) == 0)
		aPermute = 0;

	int n = P3;
	assert(pOp->p4type == P4_KEYINFO);
	assert(n > 0);
	int p1 = P1;
	int p2 = P2;

	struct key_def *def = sql_key_info_to_key_def(pOp->p4.key_info);
	if (def == NULL)
		goto abort_due_to_error;
#if SQL_DEBUG
	if (aPermute) {
		int mx = 0;
		for(uint32_t k = 0; k < (uint32_t)n; k++)
			if (aPermute[k] > mx)
				mx = aPermute[k];
		assert(p1>0 && p1+mx<=(p->nMem+1 - p->nCursor)+1);
		assert(p2>0 && p2+mx<=(p->nMem+1 - p->nCursor)+1);
    break;
}

case OP_FetchByName: {
    /* FETCHBYNAME - Fetch by name */
    struct vdbe_field_ref *ref = p->aMem[P1].u.p;
	assert(pOp->p4type == P4_DYNAMIC);
	uint32_t id;
	if (ref->format != NULL) {
		const char *name = pOp->p4.z;
		uint32_t len = strlen(name);
		uint32_t hash = field_name_hash(name, len);
		struct tuple_dictionary *dict = ref->format->dict;
		if (tuple_fieldno_by_name(dict, name, len, hash, &id) != 0) {
			diag_set(ClientError, ER_SQL_CANT_RESOLVE_FIELD, name);
			goto abort_due_to_error;
    break;
}

case OP_Fetch: {
    /* FETCH - Fetch record */
    struct vdbe_field_ref *ref = p->aMem[P1].u.p;
	struct Mem *res = vdbe_prepare_null_out(p, P3);
	if (vdbe_field_ref_fetch(ref, P2, res) != 0)
		goto abort_due_to_error;
	REGISTER_TRACE(p, P3, res);
	DISPATCH();
    break;
}

case OP_Count: {
    /* COUNT - Count records */
    /* out2 */
	i64 nEntry;
	BtCursor *pCrsr;

	assert(p->apCsr[P1]->eCurType == CURTYPE_TARANTOOL);
	pCrsr = p->apCsr[P1]->uc.pCursor;
	assert(pCrsr);
	if (pCrsr->curFlags & BTCF_TaCursor) {
		nEntry = tarantoolsqlCount(pCrsr);
    break;
}

case OP_CreateForeignKey: {
    /* CREATEFOREIGNKEY - Create foreign key constraint */
    assert(P1 >= 0);
	struct Mem *mems = &aMem[P1];
	assert(mem_is_uint(&mems[0]) && mem_is_uint(&mems[1]));
	uint32_t child_id = mems[0].u.u;
	uint32_t parent_id = mems[1].u.u;
	const char *name = pOp->p4.z;
	const char *mapping = NULL;
	uint32_t child_fieldno = 0;
	uint32_t parent_fieldno = 0;
	if (mem_is_uint(&mems[2])) {
		assert(mem_is_uint(&mems[3]));
		child_fieldno = mems[2].u.u;
		parent_fieldno = mems[3].u.u;
    break;
}

case OP_CreateCheck: {
    /* CREATECHECK - Create check constraint */
    assert(P1 >= 0 && P2 >= 0 && P3 >= 0);
	uint32_t space_id = aMem[P1].u.u;
	uint32_t func_id = aMem[P2].u.u;
	const char *name = pOp->p4.z;
	bool is_field_ck = pOp->p5 != 0;
	uint32_t fieldno = P3;
	if (sql_check_create(name, space_id, func_id, fieldno,
			     is_field_ck) != 0)
		goto abort_due_to_error;
	if (p->nChange == 0)
		p->nChange = 1;
	DISPATCH();
    break;
}

case OP_DropTupleForeignKey: {
    /* DROPTUPLEFOREIGNKEY - Drop tuple foreign key */
    assert(P1 >= 0 && pOp->p4.z != NULL);
	if (sql_tuple_foreign_key_drop(P1, pOp->p4.z) != 0)
		goto abort_due_to_error;
	assert(p->nChange == 0);
	p->nChange = 1;
	DISPATCH();
    break;
}

case OP_DropTupleCheck: {
    /* DROPTUPLECHECK - Drop tuple check */
    assert(P1 >= 0 && pOp->p4.z != NULL);
	if (sql_tuple_check_drop(P1, pOp->p4.z) != 0)
		goto abort_due_to_error;
	assert(p->nChange == 0);
	p->nChange = 1;
	DISPATCH();
    break;
}

case OP_DropFieldForeignKey: {
    /* DROPFIELDFOREIGNKEY - Drop field foreign key */
    assert(P1 >= 0 && pOp->p4.z != NULL);
	if (sql_field_foreign_key_drop(P1, P3, pOp->p4.z) != 0)
		goto abort_due_to_error;
	assert(p->nChange == 0);
	p->nChange = 1;
	DISPATCH();
    break;
}

case OP_DropFieldCheck: {
    /* DROPFIELDCHECK - Drop field check */
    assert(P1 >= 0 && pOp->p4.z != NULL);
	if (sql_field_check_drop(P1, P3, pOp->p4.z) != 0)
		goto abort_due_to_error;
	assert(p->nChange == 0);
	p->nChange = 1;
	DISPATCH();
    break;
}

case OP_AddFuncDefault: {
    /* ADDFUNCDEFAULT - Add function as default */
    assert(aMem[P1].type == MEM_TYPE_UINT);
	uint32_t space_id = aMem[P1].u.u;
	uint32_t fieldno = P3;
	assert(aMem[P2].type == MEM_TYPE_UINT);
	uint32_t func_id = aMem[P2].u.u;
	if (sql_add_default(space_id, fieldno, func_id) != 0)
		goto abort_due_to_error;
	DISPATCH();
    break;
}

case OP_CheckViewReferences: {
    /* CHECKVIEWREFERENCES - Check view references */
    assert(P1 > 0);
	pIn1 = &aMem[P1];
	uint64_t space_id = pIn1->u.u;
	assert(space_id <= INT32_MAX);
	struct space *space = space_by_id(space_id);
	assert(space != NULL);
	if (space->def->view_ref_count > 0) {
		diag_set(ClientError, ER_DROP_SPACE, space->def->name,
			 "other views depend on this space");
		goto abort_due_to_error;
    break;
}

case OP_TransactionBegin: {
    /* TRANSACTIONBEGIN - Begin transaction */
    if (in_txn()) {
		diag_set(ClientError, ER_ACTIVE_TRANSACTION);
		goto abort_due_to_error;
    break;
}

case OP_TransactionCommit: {
    /* TRANSACTIONCOMMIT - Commit transaction */
    struct txn *txn = in_txn();
	if (txn != NULL) {
		if (txn_commit(txn) != 0)
			goto abort_due_to_error;
    break;
}

case OP_TransactionRollback: {
    /* TRANSACTIONROLLBACK - Rollback transaction */
    if (box_txn()) {
		if (box_txn_rollback() != 0)
			goto abort_due_to_error;
    break;
}

case OP_TTransaction: {
    /* TTRANSACTION - Tarantool transaction */
    if (!box_txn()) {
		if (txn_begin() == NULL)
			goto abort_due_to_error;
    break;
}

case OP_IteratorOpen: {
    /* ITERATOROPEN - Open iterator */
    struct VdbeCursor *cur = p->apCsr[P1];
	if (box_schema_version() != p->schema_ver &&
	    (pOp->p5 & OPFLAG_SYSTEMSP) == 0) {
		p->expired = 1;
		diag_set(ClientError, ER_SQL_EXECUTE, "schema version has "\
			 "changed: need to re-compile SQL statement");
		goto abort_due_to_error;
    break;
}

case OP_OpenSpace: {
    /* OPENSPACE - Open space cursor */
    assert(P1 >= 0 && P1 > 0);
	struct space *space = space_by_id(P2);
	assert(space != NULL);
	mem_set_ptr(&aMem[P1], space);
	DISPATCH();
    break;
}

case OP_OpenTEphemeral: {
    /* OPENTEPHEMERAL - Open temporary table */
    assert(P1 >= 0);

	assert(pOp->p4type == P4_DYNAMIC || pOp->p4type == P4_STATIC);
	struct sql_space_info *info = pOp->p4.space_info;
	assert(info != NULL);
	struct space *space = sql_ephemeral_space_new(info);

	if (space == NULL)
		goto abort_due_to_error;
	mem_set_ptr(&aMem[P1], space);
	DISPATCH();
    break;
}

case OP_SorterOpen: {
    /* SORTEROPEN - Open sorter */
    VdbeCursor *pCx;

	assert(P1 >= 0);
	assert(P2 >= 0);
	struct key_def *def = sql_key_info_to_key_def(pOp->p4.key_info);
	if (def == NULL)
		goto abort_due_to_error;
	pCx = allocateCursor(p, P1, P2, CURTYPE_SORTER);
	if (pCx == NULL)
		goto abort_due_to_error;
	pCx->key_def = def;
	if (sqlVdbeSorterInit(pCx) != 0)
		goto abort_due_to_error;
	DISPATCH();
    break;
}

case OP_SequenceTest: {
    /* SEQUENCETEST - Test sequence counter */
    VdbeCursor *pC;
	assert(P1 >= 0 && P1 < p->nCursor);
	pC = p->apCsr[P1];
	assert(isSorter(pC));
	if ((pC->seqCount++) == 0)
		JUMP_P2();

	DISPATCH();
    break;
}

case OP_OpenPseudo: {
    /* OPENPSEUDO - Open pseudo-cursor */
    VdbeCursor *pCx;

	assert(P1 >= 0);
	assert(P3 >= 0);
	pCx = allocateCursor(p, P1, P3, CURTYPE_PSEUDO);
	if (pCx == NULL)
		goto abort_due_to_error;
	pCx->nullRow = 1;
	pCx->uc.pseudoTableReg = P2;
	assert(pOp->p5==0);
	DISPATCH();
    break;
}

case OP_Close: {
    /* CLOSE - Close cursor */
    assert(P1 >= 0 && P1 < p->nCursor);
	sqlVdbeFreeCursor(p->apCsr[P1]);
	p->apCsr[P1] = 0;
	DISPATCH();
    break;
}

case OP_Sequence: {
    /* SEQUENCE - Get sequence value */
    /* out2 */
	assert(P1>=0 && P1<p->nCursor);
	assert(p->apCsr[P1]!=0);
	pOut = vdbe_prepare_null_out(p, P2);
	int64_t seq_val = p->apCsr[P1]->seqCount++;
	mem_set_uint(pOut, seq_val);
	DISPATCH();
    break;
}

case OP_NextSystemSpaceId: {
    /* NEXTSYSTEMSPACEID - Get next system space ID */
    assert(P1 >= 0 && P3 >= 0);
	uint32_t space_id = P1;
	assert(space_id == BOX_SEQUENCE_ID || space_id == BOX_FUNC_ID);
	struct Mem *res = &p->aMem[P2];
	char key[1];
	struct tuple *tuple;
	char *key_end = mp_encode_array(key, 0);
	assert(key_end - key == 1);
	if (box_index_max(space_id, 0, key, key_end, &tuple) != 0)
		goto abort_due_to_error;
	if (tuple == NULL) {
		mem_set_uint(res, 1);
		DISPATCH();
    break;
}

case OP_NextIdEphemeral: {
    /* NEXTIDEPHEMERAL - Get next ephemeral ID */
    struct space *space = (struct space*)p->aMem[P1].u.p;
	assert(space->def->id == 0);
	uint64_t rowid;
	if (space->vtab->ephemeral_rowid_next(space, &rowid) != 0)
		goto abort_due_to_error;
	/*
	 * FIXME: since memory cell can comprise only 32-bit
	 * integer, make sure it can fit in. This check should
	 * be removed when memory cell is extended with unsigned
	 * 64-bit integer.
	 */
	if (rowid > INT32_MAX) {
		diag_set(ClientError, ER_ROWID_OVERFLOW);
		goto abort_due_to_error;
    break;
}

case OP_FCopy: {
    /* FCOPY - Frame copy */
    /* out2 */
	VdbeFrame *pFrame;
	Mem *pIn1, *pOut;
	if (p->pFrame && ((P3 & OPFLAG_SAME_FRAME) == 0)) {
		for(pFrame=p->pFrame; pFrame->pParent; pFrame=pFrame->pParent);
		pIn1 = &pFrame->aMem[P1];
    break;
}

case OP_ResetCount: {
    /* RESETCOUNT - Reset counter */
    sqlVdbeSetChanges(p->nChange);
	p->nChange = 0;
	p->ignoreRaised = 0;
	DISPATCH();
    break;
}

case OP_SorterCompare: {
    /* SORTERCOMPARE - Sorter comparison */
    VdbeCursor *pC;
	int res;
	int nKeyCol;

	pC = p->apCsr[P1];
	assert(isSorter(pC));
	assert(pOp->p4type == P4_INT32);
	pIn3 = &aMem[P3];
	nKeyCol = pOp->p4.i;
	if (sqlVdbeSorterCompare(pC, pIn3, nKeyCol, &res) != 0)
		goto abort_due_to_error;
	if (res) JUMP_P2();
	DISPATCH();
    break;
}

case OP_SorterData: {
    /* SORTERDATA - Get sorter data */
    VdbeCursor *pC;

	pOut = vdbe_prepare_null_out(p, P2);
	pC = p->apCsr[P1];
	assert(isSorter(pC));
	if (sqlVdbeSorterRowkey(pC, pOut) != 0)
		goto abort_due_to_error;
	assert(mem_is_bin(pOut));
	assert(P1 >= 0 && P1 < p->nCursor);
	p->apCsr[P3]->cacheStatus = CACHE_STALE;
	DISPATCH();
    break;
}

case OP_NullRow: {
    /* NULLROW - Null row marker */
    VdbeCursor *pC;

	assert(P1 >= 0 && P1 < p->nCursor);
	pC = p->apCsr[P1];
	assert(pC!=0);
	pC->nullRow = 1;
	pC->cacheStatus = CACHE_STALE;
	if (pC->eCurType==CURTYPE_TARANTOOL) {
		assert(pC->uc.pCursor!=0);
		sql_cursor_cleanup(pC->uc.pCursor);
    break;
}

case OP_SorterInsert: {
    /* SORTERINSERT - Insert into sorter */
    /* in2 */
	assert(P1 >= 0 && P1 < p->nCursor);
	struct VdbeCursor *cursor = p->apCsr[P1];
	assert(cursor != NULL);
	assert(isSorter(cursor));
	pIn2 = &aMem[P2];
	assert(mem_is_bin(pIn2));
	if (sqlVdbeSorterWrite(cursor, pIn2) != 0)
		goto abort_due_to_error;
	DISPATCH();
    break;
}

case OP_Clear: {
    /* CLEAR - Clear space */
    assert(P1 > 0);
	uint32_t space_id = P1;
	struct space *space = space_by_id(space_id);
	assert(space != NULL);
	if (P2 > 0) {
		if (box_truncate(space_id) != 0)
			goto abort_due_to_error;
    break;
}

case OP_ResetSorter: {
    /* RESETSORTER - Reset sorter */
    VdbeCursor *pC;

	assert(P1 >= 0 && P1 < p->nCursor);
	pC = p->apCsr[P1];
	assert(pC != 0);
	if (isSorter(pC)) {
		sqlVdbeSorterReset(pC->uc.pSorter);
    break;
}

case OP_RenameTable: {
    /* RENAMETABLE - Rename table */
    uint32_t space_id;
	struct space *space;
	char *zOldTableName;
	const char *zNewTableName;

	space_id = P1;
	space = space_by_id(space_id);
	assert(space);
	/* Rename space op doesn't change triggers. */
	struct sql_trigger *triggers = space->sql_triggers;
	assert(space->def->name != NULL);
	zNewTableName = pOp->p4.z;
	zOldTableName = sql_xstrdup(space_name(space));
	if (sql_rename_table(space_id, zNewTableName) != 0)
		goto abort_due_to_error;
	/*
	 * Rebuild 'CREATE TRIGGER' expressions of all triggers
	 * created on this table. Sure, this action is not atomic
	 * due to lack of transactional DDL, but just do the best
	 * effort.
	 */
	for (struct sql_trigger *trigger = triggers; trigger != NULL; ) {
		/* Store pointer as trigger will be destructed. */
		struct sql_trigger *next_trigger = trigger->next;
		/*
		 * FIXME: In the case of error, part of triggers
		 * would have invalid space name in tuple so can
		 * not been persisted. Server could be restarted.
		 * In this case, rename table back and try again.
		 */
		if (tarantoolsqlRenameTrigger(trigger->zName, zOldTableName,
					      zNewTableName) != 0)
			goto abort_due_to_error;
		trigger = next_trigger;
    break;
}

case OP_LoadAnalysis: {
    /* LOADANALYSIS - Load analysis */
    assert(P1 == 0);
	/* TODO: Enable analysis. */
	/*
	if (sql_analysis_load(db) != 0)
		goto abort_due_to_error;
	*/
	DISPATCH();
    break;
}

case OP_Param: {
    /* PARAM - Load parameter */
    /* out2 */
	VdbeFrame *pFrame;
	Mem *pIn;
	pOut = vdbe_prepare_null_out(p, P2);
	pFrame = p->pFrame;
	pIn = &pFrame->aMem[P1 + pFrame->aOp[pFrame->pc].p1];
	mem_copy_as_ephemeral(pOut, pIn);
	DISPATCH();
    break;
}

case OP_OffsetLimit: {
    /* OFFSETLIMIT - Calculate offset limit (r[P2]=r[P1]+r[P3]) */
    int handler_rc = vdbe_op_offsetlimit(p, pOp, aMem);
    if (handler_rc < 0)
        goto abort_due_to_error;
    if (handler_rc == 1) {
        if (pOp->opcode == OP_ResultRow)
            goto done_returning_row;
        goto jump_to_p2;
    }
    break;
}

case OP_Expire: {
    /* EXPIRE - Expire entry */
    if (!P1) {
		sqlExpirePreparedStatements();
    break;
}

case OP_GenSpaceid: {
    /* GENSPACEID - Generate space ID */
    assert(P1 > 0);
	pOut = vdbe_prepare_null_out(p, P1);
	uint32_t u;
	if (box_generate_space_id(&u, false) != 0)
		goto abort_due_to_error;
	mem_set_uint(pOut, u);
	DISPATCH();
    break;
}

case OP_SetSession: {
    /* SETSESSION - Set session */
    assert(pOp->p4type == P4_DYNAMIC);
	const char *setting_name = pOp->p4.z;
	int sid = session_setting_find(setting_name);
	if (sid < 0) {
		diag_set(ClientError, ER_NO_SUCH_SESSION_SETTING, setting_name);
		goto abort_due_to_error;
    break;
}

case OP_ShowCreateTable: {
    /* SHOWCREATETABLE - Show create table */
    struct Mem *ret = &aMem[P2];
	struct Mem *err = &aMem[P2 + 1];
	sql_show_create_table(aMem[P1].u.i, ret, err);
	DISPATCH();
    break;
}

case OP_Noop: {
    /* NOOP - No operation */
    DISPATCH();
    break;
}

case OP_Explain: {
    /* EXPLAIN - Explain query */
    DISPATCH();
    break;
}

case OP_IsNull: {
    /* ISNULL - Test for NULL */
    /* same as TK_ISNULL, jump, in1 */
	pIn1 = &aMem[P1];
	if (mem_is_null(pIn1))
		JUMP_P2();
	DISPATCH();
    break;
}

case OP_NotNull: {
    /* NOTNULL - Test for not NULL */
    /* same as TK_NOTNULL, jump, in1 */
	pIn1 = &aMem[P1];
	if (!mem_is_null(pIn1)) {
		JUMP_P2();
    break;
}

case OP_Decimal: {
    /* DECIMAL - Load decimal r[P2]=P4 */
    /* same as TK_DECIMAL, out2 */
	pOut = vdbe_prepare_null_out(p, P2);
	mem_set_dec(pOut, pOp->p4.dec);
	DISPATCH();
    break;
}

case OP_Program: {
    /* PROGRAM - Execute program */
    /* jump */
	int nMem;               /* Number of memory registers for sub-program */
	int nByte;              /* Bytes of runtime space required for sub-program */
	Mem *pRt;               /* Register to allocate runtime space */
	Mem *pMem;              /* Used to iterate through memory cells */
	Mem *pEnd;              /* Last memory cell in new array */
	VdbeFrame *pFrame;      /* New vdbe frame to execute in */
	SubProgram *pProgram;   /* Sub-program to execute */
	void *t;                /* Token identifying trigger */

	pProgram = pOp->p4.pProgram;
	pRt = &aMem[P3];
	assert(pProgram->nOp>0);

	/* If the p5 flag is clear, then recursive invocation of triggers is
	 * disabled for backwards compatibility (p5 is set if this sub-program
	 * is really a trigger, not a foreign key action, and the setting
	 * 'recursive_triggers' is not set).
	 *
	 * It is recursive invocation of triggers, at the SQL level, that is
	 * disabled. In some cases a single trigger may generate more than one
	 * SubProgram (if the trigger may be executed with more than one different
	 * ON CONFLICT algorithm). SubProgram structures associated with a
	 * single trigger all have the same value for the SubProgram.token
	 * variable.
	 */
	if (pOp->p5) {
		t = pProgram->token;
		for(pFrame=p->pFrame; pFrame && pFrame->token!=t; pFrame=pFrame->pParent);
		if (pFrame) {
			DISPATCH();
    break;
}

case OP_Sort: {
    /* SORT - Sort records */
    /* jump */
#ifdef SQL_TEST
	sql_sort_count++;
	sql_search_count--;
#endif
	/* Fall through into OP_Rewind */
#ifndef SQL_USE_GOTO
	FALLTHROUGH;
#endif /* SQL_USE_GOTO */
    break;
}

case OP_SorterSort: {
    /* SORTERSORT - Sort sorter */
    /* jump */
#ifdef SQL_TEST
	sql_sort_count++;
	sql_search_count--;
#endif
	/* Fall through into OP_Rewind */
#ifndef SQL_USE_GOTO
	FALLTHROUGH;
#endif /* SQL_USE_GOTO */
    break;
}

case OP_ShiftLeft: {
    /* SHIFTLEFT - Bitwise left shift */
    /* same as TK_LSHIFT, in1, in2, out3 */
	pIn1 = &aMem[P1];
	pIn2 = &aMem[P2];
	pOut = &aMem[P3];
	if (mem_shift_left(pIn2, pIn1, pOut) != 0)
		goto abort_due_to_error;
	assert(pOut->type == MEM_TYPE_UINT || pOut->type == MEM_TYPE_NULL);
	DISPATCH();
    break;
}

case OP_ShiftRight: {
    /* SHIFTRIGHT - Bitwise right shift */
    /* same as TK_RSHIFT, in1, in2, out3 */
	pIn1 = &aMem[P1];
	pIn2 = &aMem[P2];
	pOut = &aMem[P3];
	if (mem_shift_right(pIn2, pIn1, pOut) != 0)
		goto abort_due_to_error;
	assert(pOut->type == MEM_TYPE_UINT || pOut->type == MEM_TYPE_NULL);
	DISPATCH();
    break;
}

} /* end switch */
#endif /* SQL_USE_GOTO */